<footer class="site-foot">
  <div class="site-foot__logo">
    <a href="/">
      <svg
        width="280"
        height="280"
        viewBox="0 0 280 280"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
      >
        <!-- V: upside-down A (U shape) — slot open at top, 16.8px bottom bar -->
        <path d="M0 0 H40 V263.2 H75 V0 H115 V280 H0 Z" fill="#160000"></path>
        <!-- A: rectangular arch style -->
        <path d="M145 0 H280 V280 H240 V17 H185 V280 H145 V0 Z" fill="#160000">
        </path>
      </svg>

      <span class="u-sr-only">VA – Home</span>
    </a>
  </div><!-- .site-foot__logo -->
</footer>

<style lang="scss">
  .site-foot {
    @include flex(row, center, center);

    position: relative;
    z-index: 3;

    padding: 1.5rem;

    border-top: 1px solid color(secondary);

    @include mq(phone) {
      padding: 1rem;
    }

    &__logo {
      svg {
        display: block;
        width: 3rem;
        height: 3rem;

        @include mq(phone) {
          width: 1.875rem;
          height: 1.875rem;
        }
      }
    }
  }
</style>
